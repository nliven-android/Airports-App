<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >
    
    <Button android:id="@+id/btnGetCaliAirports"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/button_get_cali_airports" />
    
    <Button android:id="@+id/btnGetTexasAirports"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnGetCaliAirports"
        android:text="@string/button_get_texas_airports" />
    
    <ListView android:id="@+id/listAirports"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnGetTexasAirports"
        tools:listitem="@layout/airport_list_row">               
    </ListView>
    
    <!-- This TextView will be shown if there is no Airport data in the ListView -->
    
    
    

</RelativeLayout>
